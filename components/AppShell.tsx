'use client';
import Link from 'next/link';import {usePathname} from 'next/navigation';import {Menu, X, Bell, Languages, Sparkles} from 'lucide-react';import {useState} from 'react';import {navItems,utilityItems} from '@/lib/nav';
export default function AppShell({children}:{children:React.ReactNode}){const path=usePathname();const [open,setOpen]=useState(false);const items=[...navItems,...utilityItems];return <div className="min-h-screen bg-slate-50"><aside className={`fixed inset-y-0 left-0 z-50 w-72 border-r border-slate-200 bg-white transition-transform lg:translate-x-0 ${open?'translate-x-0':'-translate-x-full'}`}><div className="flex h-full flex-col"><div className="flex items-center justify-between border-b p-5"><Link href="/" className="flex items-center gap-3" onClick={()=>setOpen(false)}><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#0b1f3a] text-white font-black">SS</span><span><b className="block text-sm text-[#0b1f3a]">SAHAKAR SAHAYAK</b><small className="text-slate-500">Citizen Assistance</small></span></Link><button className="lg:hidden" onClick={()=>setOpen(false)}><X/></button></div><div className="m-4 rounded-2xl bg-gradient-to-r from-[#0b1f3a] to-[#155eef] p-4 text-white"><div className="flex items-center gap-2 text-sm font-semibold"><Sparkles size={16}/> AI civic assistant</div><p className="mt-2 text-xs leading-5 text-blue-100">Understand schemes, eligibility, documents and services in your language.</p><Link href="/assistant" onClick={()=>setOpen(false)} className="mt-3 inline-block rounded-lg bg-white/15 px-3 py-2 text-xs font-semibold">Ask Sahakar AI →</Link></div><nav className="flex-1 overflow-y-auto px-3 pb-4">{items.map(i=>{const active=path===i.href||path.startsWith(i.href+'/');return <Link key={i.href} href={i.href} onClick={()=>setOpen(false)} className={`mb-1 flex items-center gap-3 rounded-xl px-3 py-2.5 text-sm font-medium ${active?'bg-blue-50 text-blue-700':'text-slate-600 hover:bg-slate-50'}`}><i.icon size={18}/>{i.label}</Link>})}</nav><div className="border-t p-4 text-xs text-slate-500">DEMO MODE • Local-first SIH prototype</div></div></aside><div className="lg:pl-72"><header className="sticky top-0 z-40 border-b border-slate-200 bg-white/90 backdrop-blur"><div className="flex h-16 items-center justify-between px-4 sm:px-6"><button className="lg:hidden rounded-lg p-2 hover:bg-slate-100" onClick={()=>setOpen(true)}><Menu/></button><div className="hidden lg:block"><p className="text-sm font-semibold text-slate-800">Multilingual Government & Cooperative Assistance</p><p className="text-xs text-slate-500">English • हिन्दी • मराठी</p></div><div className="flex items-center gap-2"><button className="hidden sm:flex items-center gap-2 rounded-xl border px-3 py-2 text-sm"><Languages size={16}/> Auto language</button><button className="rounded-xl border p-2"><Bell size={17}/></button><Link href="/profile" className="grid h-9 w-9 place-items-center rounded-full bg-blue-100 text-sm font-bold text-blue-700">SS</Link></div></div></header><main>{children}</main></div>{open&&<button aria-label="Close menu" className="fixed inset-0 z-40 bg-slate-950/30 lg:hidden" onClick={()=>setOpen(false)}/>}</div>}
