'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {useParams} from 'next/navigation';import {ArrowLeft,Bookmark,CheckCircle2,ExternalLink,Loader2} from 'lucide-react';import {api,Scheme} from '@/lib/api';
export default function SchemeDetail(){const p=useParams();const id=Number(p.id);const [s,setS]=useState<Scheme|null>(null);const [saved,setSaved]=useState(false);useEffect(()=>{if(id)api.scheme(id).then(setS).catch(()=>setS(null))},[id]);if(!s)return <div className="mx-auto max-w-4xl p-8">{id?<div className="flex items-center gap-2 text-slate-500"><Loader2 className="animate-spin" size={18}/> Loading scheme…</div>:null}</div>;return <div className="mx-auto max-w-5xl px-4 py-7 sm:px-6"><Link href="/schemes" className="inline-flex items-center gap-2 text-sm text-slate-500"><ArrowLeft size={16}/> All schemes</Link><div className="mt-5 card rounded-3xl p-6 sm:p-8"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">{s.category}</span><span className="rounded-full bg-amber-50 px-3 py-1 text-xs font-semibold text-amber-800">DEMO KNOWLEDGE BASE</span></div><h1 className="mt-4 text-3xl font-bold text-[#0b1f3a]">{s.name}</h1><p className="mt-3 max-w-3xl text-slate-600 leading-7">{s.description}</p><div className="mt-6 flex flex-wrap gap-3"><button onClick={()=>api.save(s.id).then(()=>setSaved(true))} className="inline-flex items-center gap-2 rounded-xl bg-[#0b1f3a] px-4 py-2.5 text-sm font-semibold text-white"><Bookmark size={16}/>{saved?'Saved':'Save scheme'}</button><Link href="/eligibility" className="rounded-xl border px-4 py-2.5 text-sm font-semibold text-slate-700">Check my eligibility</Link><Link href="/assistant" className="rounded-xl border px-4 py-2.5 text-sm font-semibold text-slate-700">Ask Sahakar AI</Link></div><div className="mt-8 grid gap-6 md:grid-cols-2"><Section title="Potential eligibility" items={s.eligibility}/><Section title="Benefits" items={s.benefits}/><Section title="Documents" items={s.documents}/><Section title="Application steps" items={s.application_procedure}/></div><div className="mt-8 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm leading-6 text-amber-900"><b>Verification note:</b> {s.verification_notes} You may be eligible based on the information provided. Please verify current eligibility on the official government portal.</div><a href={s.official_url} target="_blank" rel="noreferrer" className="mt-5 inline-flex items-center gap-2 text-sm font-semibold text-blue-700">Official source <ExternalLink size={15}/></a></div></div>}
function Section({title,items}:{title:string;items:string[]}){return <section><h2 className="flex items-center gap-2 font-bold text-[#0b1f3a]"><CheckCircle2 size={17} className="text-green-600"/>{title}</h2><ul className="mt-3 space-y-2 text-sm leading-6 text-slate-600">{items.map((x,i)=><li key={i} className="rounded-xl bg-slate-50 p-3">{x}</li>)}</ul></section>}
