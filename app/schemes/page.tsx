'use client';
import {useEffect,useState} from 'react';import Link from 'next/link';import {Search,Loader2,ArrowRight} from 'lucide-react';import {api,Scheme} from '@/lib/api';
const cats=['All','Agriculture','Housing','Finance','Business'];
export default function Page(){const [items,setItems]=useState<Scheme[]>([]);const [q,setQ]=useState('');const [cat,setCat]=useState('All');const [loading,setLoading]=useState(true);useEffect(()=>{const t=setTimeout(()=>{setLoading(true);api.schemes(`?q=${encodeURIComponent(q)}&category=${encodeURIComponent(cat)}`).then(setItems).catch(()=>setItems([])).finally(()=>setLoading(false))},250);return()=>clearTimeout(t)},[q,cat]);return <div className="mx-auto max-w-6xl px-4 py-7 sm:px-6"><div className="mb-6"><span className="text-sm font-bold text-blue-700">SCHEME EXPLORER</span><h1 className="mt-1 text-3xl font-bold text-[#0b1f3a]">Government schemes, explained simply.</h1><p className="mt-2 max-w-2xl text-sm text-slate-500">Search the local knowledge base using names, categories or natural-language terms.</p></div><div className="card rounded-2xl p-4"><div className="flex items-center gap-3 rounded-xl border bg-slate-50 px-3 py-2"><Search size={18} className="text-slate-400"/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Try: farmer financial help, housing, business…" className="w-full bg-transparent text-sm outline-none"/></div><div className="mt-3 flex gap-2 overflow-x-auto">{cats.map(c=><button key={c} onClick={()=>setCat(c)} className={`rounded-full px-4 py-2 text-xs font-semibold ${cat===c?'bg-[#0b1f3a] text-white':'border bg-white text-slate-600'}`}>{c}</button>)}</div></div><div className="mt-6 flex items-center justify-between"><p className="text-sm text-slate-500">{loading?'Searching…':`${items.length} scheme${items.length===1?'':'s'} found`}</p>{loading&&<Loader2 className="animate-spin text-blue-600" size={18}/>}</div><div className="mt-3 grid gap-4 md:grid-cols-2">{items.map(s=><article key={s.id} className="card rounded-2xl p-5"><div className="flex items-start justify-between gap-3"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-[11px] font-bold text-blue-700">{s.category}</span><span className="rounded-full bg-amber-50 px-2 py-1 text-[10px] text-amber-800">VERIFY CURRENT RULES</span></div><h2 className="mt-4 text-lg font-bold text-[#0b1f3a]">{s.name}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{s.description}</p><p className="mt-3 text-xs font-semibold text-slate-700">Potentially relevant because:</p><ul className="mt-1 list-disc pl-5 text-xs leading-5 text-slate-500"><li>{s.benefits[0]}</li><li>{s.state_scope}</li></ul><Link href={`/schemes/${s.id}`} className="mt-4 inline-flex items-center gap-2 rounded-xl bg-blue-600 px-4 py-2 text-xs font-semibold text-white">View details <ArrowRight size={14}/></Link></article>)}</div></div>}
