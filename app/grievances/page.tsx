'use client';
import {useState} from 'react';import {AlertCircle,CheckCircle2} from 'lucide-react';
export default function Page(){const [form,setForm]=useState({complaint:'',department:'',location:'',reference_number:''});const [result,setResult]=useState<any>(null);const [busy,setBusy]=useState(false);const submit=async(e:any)=>{e.preventDefault();setBusy(true);setResult(null);try{const r=await fetch('/api/grievances',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(form)});setResult(await r.json())}finally{setBusy(false)}};return <div className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-8"><div className="rounded-3xl bg-gradient-to-br from-[#0b1f3a] to-[#155eef] p-7 text-white"><div className="flex items-center gap-3"><AlertCircle/><div><p className="text-xs font-bold uppercase tracking-wider text-blue-200">SAHAKAR SAHAYAK</p><h1 className="text-3xl font-black">Grievance Assistant</h1></div></div><p className="mt-3 text-sm text-blue-100">Describe a service problem and receive structured guidance for the next step.</p></div><div className="mt-6 rounded-2xl border border-amber-200 bg-amber-50 p-4 text-sm text-amber-900"><b>DEMO / GUIDANCE ONLY</b> — this prototype does not submit complaints to government systems.</div><div className="mt-5 grid gap-5 lg:grid-cols-[1fr_380px]"><form onSubmit={submit} className="card rounded-2xl p-6"><h2 className="text-lg font-bold">Complaint details</h2><label className="mt-5 block text-sm font-semibold">What happened?</label><textarea required value={form.complaint} onChange={e=>setForm({...form,complaint:e.target.value})} placeholder="Example: My scheme payment has been pending for two months." className="mt-2 min-h-36 w-full rounded-xl border p-4 text-sm outline-none focus:border-blue-500"/><div className="mt-4 grid gap-4 sm:grid-cols-2">{[['department','Department (optional)'],['location','Location (optional)'],['reference_number','Reference number (optional)']].map(([k,l])=><label key={k} className="text-sm font-semibold">{l}<input value={(form as any)[k]} onChange={e=>setForm({...form,[k]:e.target.value})} className="mt-2 w-full rounded-xl border px-4 py-3 text-sm"/></label>)}</div><button disabled={busy} className="mt-5 rounded-xl bg-[#0b1f3a] px-5 py-3 text-sm font-bold text-white disabled:opacity-50">{busy?'Analyzing…':'Analyze grievance'}</button></form>{result&&<section className="card rounded-2xl p-6"><div className="flex items-center gap-2 text-green-700"><CheckCircle2 size={19}/><b>Guidance generated</b></div><p className="mt-4 text-xs font-bold uppercase text-blue-600">{result.category}</p><h2 className="mt-1 text-xl font-black text-[#0b1f3a]">{result.summary}</h2><div className="mt-5 space-y-4 text-sm"><div><p className="font-bold">Suggested department</p><p className="mt-1 text-slate-600">{result.suggested_department}</p></div><div><p className="font-bold">Suggested next step</p><p className="mt-1 text-slate-600">{result.next_step}</p></div><div><p className="font-bold">Required evidence</p><ul className="mt-1 list-disc pl-5 text-slate-600">{result.required_evidence?.map((x:string)=><li key={x}>{x}</li>)}</ul></div></div></section>}</div></div></div>}
