'use client';
import Link from 'next/link'; import {useEffect,useState} from 'react'; import {Bookmark,Trash2,ArrowRight} from 'lucide-react'; import {api} from '@/lib/api';
export default function Page(){const [rows,setRows]=useState<any[]>([]);const [err,setErr]=useState('');const load=()=>api.saved().then(setRows).catch(e=>setErr(e.message));

useEffect(() => {
  load();
}, []);const remove=async(id:number)=>{await api.unsave(id);load()};return <div className="min-h-[calc(100vh-4rem)] bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-8"><div className="rounded-3xl bg-gradient-to-br from-[#0b1f3a] to-[#155eef] p-7 text-white"><div className="flex items-center gap-4"><Bookmark/><div><p className="text-xs font-bold uppercase tracking-wider text-blue-200">SAVED SCHEMES</p><h1 className="text-3xl font-black">Your saved schemes</h1></div></div></div>{err&&<div className="mt-5 rounded-xl bg-red-50 p-4 text-sm text-red-700">{err}</div>}<div className="mt-6 grid gap-4 md:grid-cols-2">{rows.map(s=><div key={s.id} className="card rounded-2xl p-5"><div className="flex items-start gap-3"><div className="flex-1"><span className="rounded-full bg-blue-50 px-2.5 py-1 text-xs font-bold text-blue-700">{s.category}</span><h2 className="mt-3 text-lg font-bold text-[#0b1f3a]">{s.name}</h2><p className="mt-2 text-sm leading-6 text-slate-500">{s.description}</p></div><button onClick={()=>remove(s.id)} className="rounded-lg p-2 text-slate-400 hover:bg-red-50 hover:text-red-600"><Trash2 size={17}/></button></div><div className="mt-4 flex gap-2"><Link href={`/schemes/${s.id}`} className="flex items-center gap-2 rounded-xl bg-[#0b1f3a] px-4 py-2.5 text-sm font-semibold text-white">Open <ArrowRight size={15}/></Link><Link href="/eligibility" className="rounded-xl border px-4 py-2.5 text-sm font-semibold text-slate-700">Check eligibility</Link></div></div>)}{!rows.length&&<div className="card rounded-2xl p-8 text-center text-sm text-slate-500 md:col-span-2">No saved schemes yet. <Link href="/schemes" className="font-semibold text-blue-700">Explore schemes →</Link></div>}</div></div></div>}
