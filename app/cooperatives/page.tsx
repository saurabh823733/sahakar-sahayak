'use client';
import {useEffect,useState} from 'react';import {Landmark,Search,ChevronDown} from 'lucide-react';
export default function Page(){const [q,setQ]=useState('');const [category,setCategory]=useState('all');const [rows,setRows]=useState<any[]>([]);const load=async()=>{const p=new URLSearchParams();if(q)p.set('q',q);if(category!=='all')p.set('category',category);setRows(await fetch(`/api/cooperatives?${p}`,{cache:'no-store'}).then(r=>r.json()))};useEffect(()=>{load()},[category]);return <div className="min-h-screen bg-slate-50"><div className="mx-auto max-w-6xl px-5 py-8"><div className="rounded-3xl bg-gradient-to-br from-[#0b1f3a] to-[#155eef] p-7 text-white"><div className="flex items-center gap-3"><Landmark/><div><p className="text-xs font-bold uppercase tracking-wider text-blue-200">SAHAKAR SAHAYAK</p><h1 className="text-3xl font-black">Cooperative Services</h1></div></div><p className="mt-3 text-sm text-blue-100">Search local guidance for registration, membership, documentation, finance and cooperative complaints.</p></div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><div className="relative flex-1"><Search className="absolute left-3 top-3 text-slate-400" size={18}/><input value={q} onChange={e=>setQ(e.target.value)} onKeyDown={e=>e.key==='Enter'&&load()} placeholder="Search cooperative services…" className="w-full rounded-xl border bg-white py-3 pl-10 pr-4 text-sm"/></div><div className="relative"><select value={category} onChange={e=>setCategory(e.target.value)} className="appearance-none rounded-xl border bg-white px-4 py-3 pr-10 text-sm"><option value="all">All categories</option><option>Registration</option><option>Membership</option><option>Documentation</option><option>Finance</option><option>Complaints</option><option>Cooperative schemes</option></select><ChevronDown className="pointer-events-none absolute right-3 top-3.5 text-slate-400" size={16}/></div><button onClick={load} className="rounded-xl bg-[#0b1f3a] px-5 py-3 text-sm font-bold text-white">Search</button></div><div className="mt-5 grid gap-4 md:grid-cols-2">{rows.map(r=><article key={r.id} className="card rounded-2xl p-5"><span className="rounded-full bg-blue-50 px-3 py-1 text-xs font-bold text-blue-700">{r.category}</span><h2 className="mt-3 text-lg font-bold text-[#0b1f3a]">{r.title}</h2><p className="mt-2 text-sm leading-6 text-slate-600">{r.description}</p><h3 className="mt-4 text-sm font-bold">Suggested steps</h3><ol className="mt-2 space-y-2 text-sm text-slate-600">{r.steps.map((x:string,i:number)=><li key={x}><b>{i+1}.</b> {x}</li>)}</ol><h3 className="mt-4 text-sm font-bold">Common documents</h3><p className="mt-2 text-sm text-slate-600">{r.documents.join(' • ')}</p><p className="mt-4 rounded-xl bg-amber-50 p-3 text-xs leading-5 text-amber-900">{r.notes}</p></article>)}</div></div></div>}
